import React from "react";
import { MapContainer } from "react-leaflet";
// import { Mapco as LeafletMap } from "react-leaflet";

const Map = () => {
  return <MapContainer></MapContainer>;
};

export default Map;
